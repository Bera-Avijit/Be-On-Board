package com.beonboard.entity;

/**
 * An Enum is a special "class" that represents a group of constants.
 * Here, we define the only three roles allowed in our Job Portal.
 */
public enum UserRole {
    CANDIDATE,
    RECRUITER,
    ADMIN
}
